package com.goormthon5backend.repository;

import com.goormthon5backend.domain.entity.AccommodationHostInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccommodationHostInfoRepository extends JpaRepository<AccommodationHostInfo, Long> {
}
